{
  "scripts": {
    "node:flags": "node --experimental-strip-types",
    "dev": "npm run node:flags --watch ../bin/igf.ts",
    "test": "node --test tests/*.ts"
  },
  "type": "module",
  "devDependencies": {
    "socket.io-client": "^4.8.1"
  }
}
