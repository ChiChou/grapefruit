# Frontend GUI for igf

Use `npm run dev` from this directory to start the Vite frontend.

For normal development, run `npm run dev:both` from the repository root to start the backend and GUI together. Use `npm run dev:all` when you also want the Frida agent watchers.
