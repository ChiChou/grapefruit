name: Build Single Executable Tool

on:
  workflow_dispatch:
  workflow_call:
  push:
    branches:
      - main
      - dev
    paths:
      - "src/**"
      - "agent/**"
      - "gui/**"
      - "scripts/**"
      - "externals/**"
      - "package.json"
      - "package-lock.json"
      - "tsconfig.json"
      - "tsdown.config.ts"
      - "tsdown.sea.config.ts"
      - ".github/actions/setup/**"
      - ".github/workflows/build-cli.yml"

jobs:
  assets:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: true

      - uses: ./.github/actions/setup

      - name: Install and build assets
        run: |
          npm ci
          npm ci --prefix agent
          npm ci --prefix gui

      - name: Upload application assets
        uses: actions/upload-artifact@v6
        with:
          name: application-assets
          path: |
            agent/dist
            agent/types
            gui/dist
            radare2.wasm

  build:
    needs: assets
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            name: linux-x64
          - os: windows-latest
            name: windows-x64
          - os: macos-15
            name: darwin-arm64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: true

      - uses: actions/setup-node@v6
        with:
          node-version: "26"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Download application assets
        uses: actions/download-artifact@v6
        with:
          name: application-assets
          path: .

      - name: Build executable
        run: npm run build:cli

      - name: Smoke test
        shell: bash
        run: ./build/Release/igf-${{ matrix.name }}${{ runner.os == 'Windows' && '.exe' || '' }} --help

      - name: Upload executable
        uses: actions/upload-artifact@v6
        with:
          name: cli-${{ matrix.name }}
          path: build/Release/igf-${{ matrix.name }}${{ runner.os == 'Windows' && '.exe' || '' }}

  archive:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v6
        with:
          pattern: cli-*
          path: cli
          merge-multiple: true

      - uses: actions/upload-artifact@v6
        with:
          name: cli-binaries
          path: cli/
