import {
  create, NButton, NInput, NSpace, NDivider,
  NLayout, NLayoutContent, NLayoutSider, NLayoutFooter,
  NPageHeader, NBreadcrumb, NBreadcrumbItem, NLayoutHeader,
} from 'naive-ui'

export default create({
  components: [NButton, NInput, NSpace, NDivider,
    NPageHeader, NBreadcrumb, NBreadcrumbItem,
    NLayout, NLayoutContent, NLayoutSider, NLayoutFooter, NLayoutHeader,
  ]
});
